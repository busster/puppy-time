module.exports = class Human {
  constructor ({ firstName, lastName, homes, hangouts, pets }) {
    this.firstName = firstName
    this.lastName = lastName
    this.homes = homes
    this.hangouts = hangouts
    this.pets = pets
  }
}
